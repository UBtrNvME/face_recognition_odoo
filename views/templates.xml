<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="approval_email_template" model="mail.template">
            <field name="name">Проект ${object.name} был одобрен</field>
            <field name="email_from">${object.company_id and object.company_id.email or ''}</field>
            <field name="subject">Проект ${object.name} был одобрен</field>
            <field name="email_to">${object.creator_email|safe}</field>
            <field name="lang">${object.lang}</field>
            <field name="model_id" ref="project.model_project_project"/>
            <field name="auto_delete" eval="True"/>
            <field name="body_html">
                <![CDATA[
	                <p>Уважаемый(ая) ${(object.partner_id.name)},<br/><br/>
	                    Ваш проект был одобрен!<br/></p>
                            C уважением,<br/>
                    ${(object.company_id.name)}
	            ]]>
            </field>
        </record>
        <record id="reject_email_template" model="mail.template">
            <field name="name">Проект ${object.name} был отменён</field>
            <field name="email_from">${object.company_id and object.company_id.email or ''}</field>
            <field name="subject">Проект ${object.name} был отменён</field>
            <field name="email_to">${object.creator_email|safe}</field>
            <field name="lang">${object.lang}</field>
            <field name="model_id" ref="project.model_project_project"/>
            <field name="auto_delete" eval="True"/>
            <field name="body_html">
                <![CDATA[
	                <p>Уважаемый(ая) ${(object.partner_id.name)},<br/><br/>
	                    Ваш проект был отменён!<br/></p>
                            C уважением,<br/>
                    ${(object.company_id.name)}
	            ]]>
            </field>
        </record>
    </data>
</odoo>